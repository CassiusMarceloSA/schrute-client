const Loader = () => {
  return (
    <div className="h-full w-full absolute bg-zinc-800/70 flex items-center justify-center transition-all">
      Loading...
    </div>
  );
};

export default Loader;
