const Loader = () => {
  return (
    <div className="h-full w-full overflow-hidden top-0 left-0 box-border z-50 absolute bg-zinc-800/70 flex items-center justify-center transition-all">
      Loading...
    </div>
  );
};

export default Loader;
